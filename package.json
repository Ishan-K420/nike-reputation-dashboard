{
  "name": "nike-reputation-dashboard",
  "version": "1.0.0",
  "description": "Nike Brand Reputation Monitor — Full-Stack Deployment",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node server.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
